@inherits StandardSearchComponentBase

<MudPaper Class="d-flex flex-row gap-4 pl-4 pt-1 pb-1 pr-4" Elevation="1">

    @foreach(var searchItem in SearchItems)
    {
        <CascadingValue Value="searchItemCallback">
            <DynamicComponent Type="searchItem.SearchComponent" Parameters="searchItem.Parameters" />
        </CascadingValue>
    }

    <MudSpacer />

    @if (isSearchInProgress)     
    {
        <MudProgressCircular Color="Color.Default" Indeterminate="true" />
    }
    else
    {
        <MudTooltip Text="@SearchConstants.SEARCH" Arrow="true">
            <MudIconButton Icon="@Icons.Material.Filled.Search" aria-label="Search" OnClick="OnClickAsync" Class="align-center"></MudIconButton>
        </MudTooltip>
    }

</MudPaper>