@typeparam T where T : class, new()
@inherits ListDetailBase<T> 

<div class="container container-body float-start">
	<div class="row">
		<div class="col-sm">
            @if(dynamicModel != null)
            {
                <table class="table">
                    <thead>
                        <tr>
                            <th>
                                <button type="button" class="btn btn-small btn-outline-primary fas fa-save float-left" @onclick="() => Save()"/>                                
                                <button type="button" class="btn btn-small btn-outline-danger fas fa-times float-end" @onclick="() => Delete()"/>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <DynamicComponent Type="dynamicModel.RootContainer.DynamicComponent" Parameters="dynamicModel.RootContainer.Parameters" />   
                            </td>
                        </tr>
                    </tbody>
                </table>
            }
		</div>
		<div class="col-sm">
            @if (dynamicList != null)
            {
                <table class="table table-striped">
                    <thead>
                        <tr>
                            @foreach (var column in dynamicList.ConfigItems)
                            {
                                <th>@column.Label</th>
                            }
                            <th> 
                                <button type="button" class="btn btn-small btn-outline-primary fas fa-plus float-left" @onclick="() => New()"/>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var listIitem in dynamicList.DynamicListItems)
                        {
                            <tr>
                                @foreach (var column in dynamicList.ConfigItems)
                                {
                                    <td>
                                        @dynamicList.GetValue(listIitem.Model, column.PropertyName)
                                    </td>
                                }
                                <td>
                                    <button type="button" class="btn btn-small btn-outline-primary fas fa-edit" @onclick="() => Edit(dynamicList.GetValue(listIitem.Model, dynamicList.Config.NavigateToProperty))"/>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
		</div>
	</div>
</div>
