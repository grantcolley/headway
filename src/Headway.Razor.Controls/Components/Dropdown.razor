@using Headway.Core.Dynamic
@using Headway.Core.Model
@using Microsoft.AspNetCore.Components.Forms
@using System.Reflection
@using System.Linq.Expressions

@inherits DropdownBase

<div class="mb-1">
    <div class="row">
        <div class="col-sm-4 col-form-label">
            @if(string.IsNullOrWhiteSpace(Field.Tooltip))
            {
                @Field.Label                
            }
            else
            {
                <ComponentTip Text="@Field.Tooltip">@Field.Label</ComponentTip>    
            }
        </div>
        <div class="col-sm-8">
            <MudSelect  T="OptionItem" Variant="Variant.Outlined" AnchorOrigin="Origin.BottomCenter" MultiSelection="false"
                        @bind-Value="SelectedItem" SelectedValuesChanged="OnValueChanged">
                @if (optionItems != null)
                {
                    @foreach (var optionItem in optionItems)
                    {
                        <MudSelectItem T="OptionItem" Value="@optionItem"/>
                    }
                }
            </MudSelect>
        </div>
    </div>
</div>