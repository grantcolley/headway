@page "/alert/{AlertType}/{Title}/{Message}"

@if (string.IsNullOrWhiteSpace(Title))
{
    <MudAlert Severity="@severity"><b>@Title</b>&nbsp;@Message</MudAlert>
}
else
{
    <MudAlert Severity="@severity">@Message</MudAlert>
}