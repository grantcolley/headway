@using System

@page "/Page/{Config}/{Id:int?}"
@inherits PageBase
<AuthorizeView>
    <Authorized Context="page">
        @if (config == null)
        {
            <Loading Message="Loading..."/>
        }
        else
        {
            @RenderView()
        }
    </Authorized>
    <NotAuthorized>
        <LoginMessage/>
    </NotAuthorized>
</AuthorizeView>