@using Microsoft.AspNetCore.Components.Forms

@typeparam T where T : class, new()
@inherits CardBase<T>

@if (dynamicModel == null)
{
    <p><em>Loading...</em></p>
}
else
{
    @if(@Alert == null)
    {
        <div class="container container-body float-start">
            <div class="lead">
                <h4>@dynamicModel.Config.Title</h4>
            </div>
            <div class="row">
                <div class="col-sm">
                    <div class="card">
                        <EditForm Model="@dynamicModel.Model" OnValidSubmit="@Submit">
                            <div class="card-header">
                                <div class="clearfix">
                                    @if (isSaveInProgress)
                                    {
                                        <i class="fas fa-spinner fa-spin fa-2x float-left"></i>
                                    }
                                    else if (!isDeleteInProgress)
                                    {
                                        <button type="submit" class="btn btn-small borderless btn-outline-primary fas fa-save float-left"/>
                                    }
                                    @if (isDeleteInProgress)
                                    {
                                        <i class="fas fa-spinner fa-spin fa-2x float-end"></i>
                                    }
                                    else if (!isSaveInProgress)
                                    {
                                        <button type="button" class="btn btn-small borderless btn-outline-danger fas fa-times float-end" @onclick="() => Delete()"/>
                                    }
                                </div>
                            </div>
                            <div class="card-body">
                                @foreach(var rootContainer in dynamicModel.RootContainers)
                                {
                                    <DynamicComponent Type="rootContainer.DynamicComponent" Parameters="rootContainer.Parameters" />   
                                }
                                </div>
                            </EditForm>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <Alert AlertType="@Alert.AlertType" Title="@Alert.Title" Message="@Alert.Message" RedirectText="@Alert.RedirectText" RedirectPage="@Alert.RedirectPage"/>
    }
}