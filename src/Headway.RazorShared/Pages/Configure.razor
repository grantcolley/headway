@page "/configure"
@inherits ConfigureBase

<AuthorizeView>
    <Authorized Context="authConfigure">
        <h3>Configure</h3>

        <hr />

@*        <div class="form-group row">
            <label class="col-sm-2 col-form-label">
                Model
            </label>
            <div class="col-sm-10">
                <select id="models">
                    <option></option>
                    @foreach (var model in models)
                    {
                        <option value="@model.Name">@model.Name</option>
                    }
                </select>
            </div>
        </div>*@

        <div class="form-group row">
            <label class="col-sm-2 col-form-label">
                Container
            </label>
            <div class="col-sm-10">
                <select id="containers">
                    <option></option>
                    @foreach (var container in containers)
                    {
                        <option value="@container.Name">@container.Name</option>
                    }
                </select>
            </div>
        </div>

        <!-- list of configs for selected container -->




    </Authorized>
    <NotAuthorized>
        <p>Please log in.</p>
    </NotAuthorized>
</AuthorizeView>